---
name: project-brief
description: 快速恢复指定项目上下文、接续任务并维护持久化摘要；支持 Git 上下文节点的保存、查看、恢复、删除和证据变更检测。适用于项目交接、继续上次的问题、管理项目记忆节点。
---

# 项目上下文恢复

## 简短指令

接受“初始化 / init”“继续”“保存”“节点”“查看 <ID>”“恢复 <ID>”“删除 <ID>”等自然语言指令，无需用户描述内部文件或脚本。初始化时读取 [references/initialization.md](references/initialization.md)，按步骤交互；这也是已经开发很久的项目首次接入本技能的入口。已有上下文再次初始化时保留有效记录，按增量接入处理。

“继续”支持可选的尾随请求：
- 只有 `$project-brief 继续`：恢复并核验当前项目上下文，向用户简要报告项目目标、当前阶段、最近完成、当前任务和下一步；不要自行选择或执行新的开发工作。
- `$project-brief 继续，<用户要求>` 或含义相同的自然语言：先恢复并核验项目上下文，再把尾随内容作为本轮最新要求执行。它优先于历史任务描述；若它与旧验收标准冲突，先更新 CURRENT_TASK.md，再执行。
- 尾随请求若信息充分，不要求用户再次确认，不把“恢复完成”作为停点。缺少的信息只有在确实阻碍正确执行时才逐步询问。

确定用户指定的项目根目录；多项目且无法判断时询问，不把当前工作目录自动当作目标仓库。
遵循该项目适用的 AGENTS.md。读取根目录的 PROJECT_CONTEXT.md 和 CURRENT_TASK.md；不存在时说明缺失，先通过 README、目录索引和当前任务相关入口建立最小摘要，勿递归通读项目。

恢复时：
- 先提取项目目标、当前需求、验收标准、问题状态及下一步。用户本轮指令优先，历史需求不能当作新授权。
- “当前问题”是可选状态，只有实际出现、被用户提出或经证据发现后才记录。没有问题时不要求用户提供，不制造缺陷或假设；初始化只需明确目标和验收标准，具体任务随工作演进。
- 摘要是导航，不是源码真相。检查记录的版本、日期及关联文件是否改变；有 Git 时参考当前状态和与记录版本的相关差异，无 Git 时检查关联文件。只核验影响当前任务的证据，不因无关变更重读全库。
- 用关键词搜索定位关联模块，再读取必要片段。摘要与源码或测试冲突时以当前证据修正摘要；未知原因写“待验证”，不要把假设写成事实。
- 简要说明当前理解、依据和仍待确认的关键点，然后继续任务。只有阻碍正确执行的信息缺失才需要追问。
- 有尾随请求时，恢复说明保持精简，并在同一轮继续完成请求；没有尾随请求时，到状态汇报结束，不根据历史“下一步”擅自开工。

完成一个工作阶段时，更新以上两个文件：只保留有效事实、明确决策、实际验证及下一步，替换失效状态。重要结论标注来源路径、符号或测试及核验日期；不复制整段会话，不存凭据。PROJECT_CONTEXT.md 保存稳定信息和模块索引，CURRENT_TASK.md 保存易变状态；多任务时明确任务 ID，不覆盖其他任务状态。摘要长度以能准确路由为准，建议稳定摘要约 600–1200 中文字，当前任务约 300–600 字；复杂细节放项目已有文档并提供路径。

首次建立摘要可使用 [references/context-template.md](references/context-template.md)。初始化节点、阶段保存及用户要求查看/恢复/删除节点时，读取 [references/git-nodes.md](references/git-nodes.md)，调用 scripts/context_nodes.py。完成有实质进展的阶段后，更新摘要并保存节点；记录相关源码证据哈希，不按消息频率存档。已有节点默认不全量读取。恢复摘要不等于回退源码；范围不明先展示节点并澄清。
